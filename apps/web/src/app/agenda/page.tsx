// La vista AGENDA la monta el AppShell (app/providers.tsx) y persiste entre
// navegaciones (la llamada de voz no se corta); esta página solo marca /agenda.
export default function AgendaPage() {
  return null;
}
