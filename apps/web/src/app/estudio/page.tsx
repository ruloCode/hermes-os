// La vista ESTUDIO la monta el AppShell (app/providers.tsx) y persiste entre
// navegaciones (la llamada de voz no se corta); esta página solo marca /estudio.
export default function EstudioPage() {
  return null;
}
