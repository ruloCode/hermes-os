// La vista FINANZAS la monta el AppShell (app/providers.tsx) y persiste entre
// navegaciones (la llamada de voz no se corta); esta página solo marca /finanzas.
export default function FinanzasPage() {
  return null;
}
