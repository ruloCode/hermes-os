// La vista INGLÉS la monta el AppShell (app/providers.tsx) y persiste entre
// navegaciones (la llamada con el tutor no se corta); esta página solo marca /ingles.
export default function InglesPage() {
  return null;
}
