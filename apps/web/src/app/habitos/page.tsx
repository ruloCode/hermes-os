// La vista HÁBITOS la monta el AppShell (app/providers.tsx) y persiste entre
// navegaciones (la llamada de voz no se corta); esta página solo marca /habitos.
export default function HabitosPage() {
  return null;
}
